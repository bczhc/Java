package Z;

import java.io.File;
import java.security.NoSuchAlgorithmException;

public class compareFile {
    boolean CompareFile(File a, File b) throws NoSuchAlgorithmException {
        boolean r = false;
        if (a.exists() && b.exists()) {
        }
        return r;
    }
}
